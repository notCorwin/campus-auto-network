## 需求

1. 自动登录校园网`login.csust.edu.cn`，以获取网络。
2. 在非校园网的环境中保持静默；监测到切换到校园网且未登录时，立即登录校园网。
3. 如果登录不成功，在获取到结果后立即重试，直到成功。
   1. 可以在特殊情况下不重试，如账号密码错误。
4. 监测网络环境变化的实现应由事件驱动；不要使用轮询。
5. 有时即使连着校园网，但已经失去了对互联网的连接。需要由事件驱动的监测，实时保证登录掉了能立刻再登录，不使用轮询。
6. 自动兼容机器的代理环境，在有无代理的情况下均能成功登录（除非是本机网络设置问题）。
7. 纯 Swift App，支持 macOS 13+。
8. 尽可能避免中间人/恶意双胞胎攻击的风险，同时保证能够自动登录。
9. 所有配置均使用 UserDefaults，完全不使用 Keychain。
10. 通过 GitHub Releases 自动更新 App 的最新提交。
11. 随开机自动启动，并保持后台运行。
12. 每隔 3 分钟获取一次软件的最新版本。

## 校园网登录成功的标志

1. 能访问 login.csust.edu.cn
2. https://www.cloudflare.com/cdn-cgi/trace 能联通
3. https://www.google.com/generate_204 返回 204

## 菜单栏组件 显示逻辑

1. 当校园网登录成功时，显示：🛰️
2. 否则显示：💥
3. 不使用 SF Symbols。

## 切换到校园网的证据

1. SSID 相同（CSUST-Student）。

## 备注

1. 暂时弃用 IPv4 地址段检查。